---
import Checkmark from "../icons/checkmark.svg";
import Bubble from "./Bubble.astro";
---

<section>
  <figure>
    <img class="en" src="../img/Pattern.png" alt="" />
    <img class="to" src="../img/whatIndex.webp" alt="mand" />
  </figure>
  <div class="text">
    <Bubble text="WE'LL WORK WITH YOU" variant="primary" />
    <h2>What To Expect</h2>
    <p class="info">Our collaboration is built on transparency, efficiency, and measurable results. Together, we create strategies tailored to your unique needs and goals.</p>
    <ul class="list">
      <li><Checkmark class="check" />Collaborate to build actionable strategies that drive sustainable growth and deliver tangible outcomes.</li>
      <li><Checkmark class="check" />Identify opportunities to optimize processes and implement impactful solutions with measurable results.</li>
    </ul>
  </div>
</section>

<style>
  @layer components {
    section {
      display: grid;
      grid-column: content;
      grid-template-columns: subgrid;
      gap: 2rem;
      /* margin: 5% 0 0 0; */
      margin-block: 4rem;

      figure {
        max-width: 46rem;
        border-radius: var(--button-radius);
        position: relative;
        padding: 3rem 2rem 0;

        .en {
          position: absolute;
          top: 0;
          left: 0;
          z-index: -2;
        }
        &::before {
          content: "";
          background-color: var(--accent-color);
          width: 164px;
          height: 183px;
          position: absolute;
          top: 8%;
          left: 3%;
          z-index: -1;
          border-radius: var(--button-radius);
        }
        .to {
          width: 100%;
          z-index: 1;
          height: 20rem;
          object-fit: cover;
          object-position: 0% 30%;
          border-radius: inherit;
        }
      }
      .text {
        display: flex;
        flex-direction: column;
        h2 {
          font-size: var(--s-font);
          font-weight: 700;
          color: var(--primary-color);
          padding-bottom: 1rem;
        }
        .info {
          color: var(--primary-color);
          padding-bottom: 1rem;
        }
        ul {
          display: grid;
          list-style: none;
          padding-left: 0;
          gap: 1rem;

          li {
            display: flex;
            align-items: center;
            gap: 1rem;

            .check {
              color: var(--accent-color);
            }
          }
        }
      }
    }
  }
</style>
